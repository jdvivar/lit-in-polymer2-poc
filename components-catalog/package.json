{
  "name": "components-catalog",
  "version": "1.0.0",
  "author": "Daniel Vivar",
  "license": "MIT",
  "scripts": {
    "build:index": "node build-index.js",
    "build:modules": "node copy-es5-node-modules.js",
    "build": "npm run build:index && polymer build && npm run build:modules"
  },
  "devDependencies": {
    "fs-extra": "8.1.0",
    "jsdom": "15.1.1",
    "polymer-cli": "1.9.11"
  }
}
